<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.example</groupId>
  <artifactId>eclipse-maven-split</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>Eclipse/Maven Split PoC</name>
  <description>
    Proof-of-concept: build Eclipse/Xtext core plugins as pure Maven jars,
    then consume them from Tycho-built UI plugins.
  </description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>21</maven.compiler.release>

    <!-- Eclipse dependency versions (from Maven Central) -->
    <xtext.version>2.42.0</xtext.version>
    <emf.ecore.version>2.42.0</emf.ecore.version>
    <eclipse.core.runtime.version>3.34.200</eclipse.core.runtime.version>

    <!-- Build tool versions -->
    <tycho.version>5.0.2</tycho.version>
    <bnd.version>7.1.0</bnd.version>
    <junit.version>5.12.2</junit.version>
  </properties>

  <!-- Core modules always built -->
  <modules>
    <module>com.example.model</module>
    <module>com.example.core</module>
    <module>com.example.dsl</module>
  </modules>

  <profiles>
    <!-- Active by default: adds Tycho/Eclipse modules to the reactor.
         Deactivated when any profile is explicitly specified (-P core-only). -->
    <profile>
      <id>with-eclipse</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <modules>
        <module>com.example.target</module>
        <module>com.example.ui</module>
        <module>com.example.ui.test</module>
      </modules>
      <build>
        <plugins>
          <plugin>
            <groupId>org.eclipse.tycho</groupId>
            <artifactId>tycho-maven-plugin</artifactId>
            <version>${tycho.version}</version>
            <extensions>true</extensions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <!-- Activating this profile deactivates with-eclipse, building only core. -->
    <profile>
      <id>core-only</id>
    </profile>
  </profiles>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.eclipse.xtext</groupId>
        <artifactId>org.eclipse.xtext</artifactId>
        <version>${xtext.version}</version>
      </dependency>
      <dependency>
        <groupId>org.eclipse.emf</groupId>
        <artifactId>org.eclipse.emf.ecore</artifactId>
        <version>${emf.ecore.version}</version>
      </dependency>
      <dependency>
        <groupId>org.eclipse.platform</groupId>
        <artifactId>org.eclipse.core.runtime</artifactId>
        <version>${eclipse.core.runtime.version}</version>
      </dependency>
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter</artifactId>
        <version>${junit.version}</version>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <!-- Tycho target platform: resolve pom-first (jar) artifacts -->
        <plugin>
          <groupId>org.eclipse.tycho</groupId>
          <artifactId>target-platform-configuration</artifactId>
          <version>${tycho.version}</version>
          <configuration>
            <pomDependencies>consider</pomDependencies>
            <target>
              <artifact>
                <groupId>com.example</groupId>
                <artifactId>com.example.target</artifactId>
                <version>${project.version}</version>
              </artifact>
            </target>
          </configuration>
        </plugin>
        <plugin>
          <groupId>biz.aQute.bnd</groupId>
          <artifactId>bnd-maven-plugin</artifactId>
          <version>${bnd.version}</version>
          <executions>
            <execution>
              <goals><goal>bnd-process</goal></goals>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>3.4.2</version>
          <configuration>
            <archive>
              <manifestFile>${project.build.outputDirectory}/META-INF/MANIFEST.MF</manifestFile>
            </archive>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
